# やること
drawer.zipをダウンロードしてください。
# プログラムについて
scriptの中にある２つのファイルが、もし動かなかったら違うかもしれないから、コピペしたらいい気がする

また、スコアを計算するプログラムに

コアを計算するプログラムに追加してもらうものとして
変数として public ScoreController scoreController; を宣言してもらって、そこにこのスクリプトをアタッチしたオブジェクトを入れてもらう
あとは、スコアを計算するプログラムの中で、scoreController.SetScore(計算したスコア); と呼び出してもらえれば、スコアが更新されるようになります。
# メモ
trashには、一応予備の内容が入ってる。

もしこのdrawer.zipが使えなかったら、trash/drawer.unitypackageがその中では最新版。

でも、エラーがあるはずだから、開いてplayer infoのmusic controller(?)みたいなのを消す必要ある
